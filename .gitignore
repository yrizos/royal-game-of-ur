# Rust
/target/

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
Icon
.fseventsd
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# VSCode
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace
.history/

# rust-analyzer
.rust-analyzer/

# Cargo.lock is committed because this workspace will contain a binary crate (ur-desktop)
# when it is added. Library-only workspaces conventionally omit it.
