[package]
name = "ur-core"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.8"

[features]
default = []
serde = ["dep:serde"]

[dependencies.serde]
version = "1"
features = ["derive"]
optional = true
